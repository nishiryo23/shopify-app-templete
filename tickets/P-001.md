# P-001 Embedded shell baseline

## Goal
Implement the embedded Polaris/App Bridge shell for `/app` and related document routes.

## Acceptance
- `/app`, `/app/pricing`, `/app/welcome` render embedded shells
- merchant-facing UI uses Polaris
- admin-outside access returns to admin context
