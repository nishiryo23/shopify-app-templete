# P-002 Session token auth

## Goal
Implement authenticated JSON API routes with correct invalid-session behavior.

## Acceptance
- `/api/context` requires bearer session token
- invalid XHR returns 401 + retry header
- invalid document requests bounce
