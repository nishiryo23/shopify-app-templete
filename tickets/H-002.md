# H-002 Architecture lint and hard guards

## Goal
Add deterministic repo rules that prevent common Codex mistakes.

## Deliverables
- architecture lint rules
- guard against direct Shopify API calls outside approved layer
- guard against productSet usage
- guard against logging tokens/raw CSV/raw webhook bodies

## Acceptance
- failing examples are caught in CI
- approved paths remain allowed

## Required checks
- `pnpm lint:arch`
- `pnpm test:unit`
