# P-003 Shop bootstrap and scopes

## Goal
Bootstrap the shop record and scope truth from authenticated app entry.

## Acceptance
- initial authenticated load creates/updates the shop row
- scope truth comes from `currentAppInstallation.accessScopes`
