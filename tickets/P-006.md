# P-006 Uninstall and redact lifecycle

## Goal
Implement app/uninstalled and shop/redact cleanup behavior.

## Acceptance
- uninstall stops writes and removes tokens
- redact schedules and completes hard delete per spec
