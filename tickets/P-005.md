# P-005 Webhook ingress and inbox

## Goal
Implement HTTPS app-specific webhook ingress with HMAC validation, dedupe, and enqueue-only processing.

## Acceptance
- invalid HMAC returns 401
- duplicate event returns 200 no-op
- enqueue happens before 200
